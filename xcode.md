# Installing xcode cli only

```bash
$ xcode-select --print-path
# Output: the result of the above command /Library/Developer/CommandLineTools

$ sudo rm -r -f /Library/Developer/CommandLineTools

$ xcode-select —install
```
